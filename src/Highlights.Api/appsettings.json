{
    "Logging": {
        "LogLevel": {
            "Default": "Information",
            "Microsoft.AspNetCore": "Warning"
        }
    },
    "AllowedHosts": "*",
    "ConnectionStrings": {
        // Local dev: when running Highlights.Api directly with `dotnet run`.
        "HighlightsDatabase": "Host=localhost;Port=5432;Database=highlights;Username=postgres;Password=postgres"
    }
}
